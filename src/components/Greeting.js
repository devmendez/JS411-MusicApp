import React from 'react';

const Greeting = props => {
  return (
    <h1 style={{ textAlign: "center", fontSize: "25px", color: "gray" }}>
      Welcome User!
    </h1>
  );
};

export default Greeting;